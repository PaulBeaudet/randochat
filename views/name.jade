//-- name.jade ~ Copyright 2015 Paul Beaudet
extends ./header.jade

block content
    // nav at top of page
    include navbar.jade
    // user gives there name
    #signup.jumbotron
        h1 What is your name?
        form(method='post')
            // input(type='hidden', name="_csrf", value=csrfToken)
            .form-group
                label(for='usr') NAME
                input#signupEmail.form-control(type='text', name='name', required='ture')
            input#submit.btn.btn-lg.btn-success(type='submit', value='submit')
include dependencies.jade
